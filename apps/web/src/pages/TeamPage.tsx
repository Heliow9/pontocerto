import {useState} from "react";
import {api} from "../api";
import {Modal} from "../components/Modal";
import {Field,modules,SaveForm,ResourceState,useResource} from "./CommercialUi";
export function TeamPage(){const r=useResource("/team"),[form,setForm]=useState<any>(null);return <><div className="commercial-heading"><div><h1>Master e supervisores</h1><p>O Master administra a empresa e define o acesso individual de cada supervisor.</p></div><button className="primary" onClick={()=>setForm({name:"",email:"",password:"",active:true,permissions:Object.fromEntries(Object.keys(modules).map(k=>[k,k==="logs"?"none":"read"]))})}>Novo supervisor</button></div><ResourceState resource={r}><div className="table-scroll"><table><thead><tr><th>Nome</th><th>E-mail</th><th>Status</th><th>Acesso</th></tr></thead><tbody>{r.data?.map((u:any)=><tr key={u.id}><td>{u.name}</td><td>{u.email}</td><td>{u.active?"Ativo":"Desativado"}</td><td><button onClick={()=>setForm({...u,active:Boolean(u.active),password:""})}>Editar permissões</button></td></tr>)}</tbody></table></div>{r.data?.length===0&&<p>Nenhum supervisor cadastrado.</p>}</ResourceState>{form&&<Modal title={form.id?"Editar supervisor":"Novo supervisor"} onClose={()=>setForm(null)} wide><SaveForm onSave={async()=>{const d={...form,password:form.password||undefined};if(form.id)await api.put(`/team/${form.id}`,d);else await api.post("/team",d);setForm(null);await r.reload();}}><div className="commercial-grid">{["name","email","password"].map(k=><Field key={k} label={k==="name"?"Nome":k==="email"?"E-mail":form.id?"Nova senha (vazio mantém a atual)":"Senha inicial"}><input type={k==="password"?"password":k==="email"?"email":"text"} required={k!=="password"||!form.id} minLength={k==="password"?8:undefined} value={form[k]} onChange={e=>setForm({...form,[k]:e.target.value})} autoComplete={k==="password"?"new-password":"off"}/></Field>)}</div><label><input type="checkbox" checked={form.active} onChange={e=>setForm({...form,active:e.target.checked})}/>Acesso ativo</label><h2>Permissões por módulo</h2><div className="commercial-grid">{Object.entries(modules).map(([k,l])=><Field key={k} label={l}><select value={form.permissions[k]} onChange={e=>setForm({...form,permissions:{...form.permissions,[k]:e.target.value}})}><option value="none">Sem acesso</option><option value="read">Consultar</option><option value="write">Consultar e alterar</option></select></Field>)}</div></SaveForm></Modal>}</>;}
