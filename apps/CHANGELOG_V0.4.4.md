# v0.4.4

- selfie obrigatória antes de cada marcação feita pelo app;
- câmera frontal com enquadramento;
- pré-visualização antes de confirmar;
- opção de tirar outra foto;
- ponto não é gravado se a selfie não chegar à API;
- arquivo de selfie vinculado ao `time_entry_id`;
- hash SHA-256 e tamanho do arquivo armazenados;
- fotos protegidas por autenticação e isolamento multi-tenant;
- botão `Ver foto` no dashboard de Pontos;
- evidência `📷 foto` no histórico do app;
- migration `007_time_entry_selfies.sql`;
- Expo Camera adicionado ao SDK 57.
